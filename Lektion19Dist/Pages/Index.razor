@page "/"
@using Lektion19Dist.Classes;

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

@code{
    protected override void OnInitialized()
    {
        var store = new BooksStore();

        //var books = store.GetBooks();
        var books = store.GetEBooks();
    }
}
